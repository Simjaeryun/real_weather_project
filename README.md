# FSD User Management

Feature Sliced Design 아키텍처 기반 사용자 관리 애플리케이션

## 🚀 프로젝트 실행 방법

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 3. 프로덕션 빌드

```bash
npm run build
npm start
```

## ✨ 구현한 기능

### 1. 사용자 목록 조회

- JSONPlaceholder API를 통한 사용자 데이터 fetch
- 반응형 그리드 레이아웃 (모바일: 1열, 태블릿: 2열, 데스크톱: 3열)
- 카드 형태의 사용자 정보 표시
- 로딩 스피너 및 에러 핸들링

### 2. 사용자 상세 정보 모달

- 사용자 카드 클릭 시 상세 정보 모달 표시
- 개별 사용자 데이터 lazy loading
- 모달 외부 클릭 또는 닫기 버튼으로 닫기

### 3. 반응형 디자인

- Tailwind CSS를 활용한 모바일 우선 디자인
- 다양한 화면 크기에 최적화된 레이아웃
- 터치 친화적인 UI 요소

## 🏗️ 기술적 의사결정 및 이유

### 1. FSD (Feature Sliced Design) 아키텍처

프로젝트가 크지 않기 때문에 FSD의 핵심 레이어만 선택적으로 사용:

```
/app           - Next.js App Router, providers, 레이아웃
/features      - 비즈니스 기능 (user-list)
/entities      - 비즈니스 엔티티 (user)
/shared        - 공통 코드 (ui, api, lib)
```

**이유:**

- 명확한 의존성 방향 (하위 레이어 → 상위 레이어)
- 각 레이어의 책임이 명확하여 유지보수 용이
- 확장 가능한 구조 (필요시 widgets, pages 레이어 추가 가능)
- 코드 재사용성 증가

### 2. Tanstack Query (React Query)

서버 상태 관리를 위해 Tanstack Query 사용:

```typescript
export const useUsers = () => {
  return useQuery({
    queryKey: ["users"],
    queryFn: userApi.getUsers,
  });
};
```

**이유:**

- 자동 캐싱, 리패칭, 동기화
- 로딩 및 에러 상태 관리 간소화
- 서버 상태와 클라이언트 상태의 명확한 분리
- Optimistic updates 및 무한 스크롤 등 확장 가능

### 3. ky HTTP 클라이언트

axios 대신 ky 선택:

```typescript
export const apiClient = ky.create({
  prefixUrl: "https://jsonplaceholder.typicode.com",
  timeout: 10000,
});
```

**이유:**

- 최신 fetch API 기반으로 가볍고 빠름
- 자동 JSON 변환 및 에러 핸들링
- TypeScript 지원 우수
- 브라우저 네이티브 API 활용

### 4. Functional Components + TypeScript

모든 컴포넌트를 함수형 컴포넌트로 작성하고 엄격한 타입 지정:

```typescript
interface UserCardProps {
  user: User;
  onClick?: () => void;
}

export function UserCard({ user, onClick }: UserCardProps) {
  // ...
}
```

**이유:**

- React 19의 최신 기능 활용
- Hooks를 통한 로직 재사용
- TypeScript로 타입 안정성 보장
- 코드 가독성 및 유지보수성 향상

### 5. Tailwind CSS

스타일링을 위해 Tailwind CSS 사용:

**이유:**

- 유틸리티 퍼스트 접근으로 빠른 개발
- 반응형 디자인 구현 간편 (`md:`, `lg:` 등)
- 일관된 디자인 시스템
- 프로덕션 빌드 시 사용하지 않는 CSS 자동 제거

### 6. 레이어별 Public API (index.ts)

각 모듈에서 index.ts를 통해 외부로 노출할 것만 export:

```typescript
// entities/user/index.ts
export { useUsers, useUser } from "./model/queries";
export { UserCard } from "./ui/user-card";
export type { User } from "./model/types";
```

**이유:**

- 모듈의 인터페이스를 명확하게 정의
- 내부 구현 변경 시 외부 영향 최소화
- import 경로 간소화

## 🛠️ 사용한 기술 스택

### Core

- **Next.js 16.1** - React 프레임워크, App Router 사용
- **React 19.2** - UI 라이브러리
- **TypeScript 5** - 정적 타입 검사

### 상태 관리 & 데이터 Fetching

- **Tanstack Query 5.62** - 서버 상태 관리
- **ky 1.7** - HTTP 클라이언트

### 스타일링

- **Tailwind CSS 4** - 유틸리티 퍼스트 CSS 프레임워크

### 개발 도구

- **ESLint** - 코드 품질 및 일관성 유지
- **PostCSS** - CSS 후처리

## 📁 프로젝트 구조

```
fsd-project/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 루트 레이아웃
│   ├── page.tsx           # 홈 페이지
│   ├── providers.tsx      # React Query Provider
│   └── globals.css        # 글로벌 스타일
├── features/              # 기능 레이어
│   └── user-list/         # 사용자 목록 기능
│       ├── ui/
│       │   ├── user-list.tsx
│       │   └── user-detail-modal.tsx
│       └── index.ts
├── entities/              # 엔티티 레이어
│   └── user/              # 사용자 엔티티
│       ├── api/           # API 호출
│       ├── model/         # 타입, 쿼리
│       ├── ui/            # UI 컴포넌트
│       └── index.ts
└── shared/                # 공유 레이어
    ├── api/               # API 클라이언트
    ├── lib/               # 유틸리티
    └── ui/                # 공통 UI 컴포넌트
        ├── button.tsx
        ├── card.tsx
        └── spinner.tsx
```

## 🎯 아키텍처 원칙

1. **단방향 의존성**: 상위 레이어는 하위 레이어만 의존
   - `features` → `entities` → `shared`
   - `entities`는 다른 `entities`에 의존 가능

2. **Public API**: 각 슬라이스는 index.ts를 통해 명시적으로 export

3. **격리성**: 각 feature는 독립적으로 개발/테스트 가능

4. **확장성**: 새로운 feature 추가 시 기존 코드 수정 최소화

## 📱 반응형 브레이크포인트

- **모바일**: < 768px (1열 그리드)
- **태블릿**: 768px - 1024px (2열 그리드)
- **데스크톱**: > 1024px (3열 그리드)
